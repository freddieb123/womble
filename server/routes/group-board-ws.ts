import type { Server } from "http";

// Stub — WebSocket broadcast for group board (implementation pending)
export function setupGroupBoardWS(_server: Server) {}

export async function broadcastBoardState(_configId: number) {}
